---
type: Rule
---

# 知识库维护

新增、修改、移动或删除笔记时执行本规则。沿用用户的实际命名和目录语义；用户调整结构后修正相关指针，继续使用新布局。

## 属性与正文

按 [笔记属性规则]({{property_rule_path}}) 判断字段，保留正文和有效的既有属性。内容、格式整理和知识核验分别处理，不能通过更新时间或状态制造新的知识证据。

保留原有 Wiki 链接、块引用和嵌入效果。移动或改名时核对实际目标，并检查入链、出链、来源路径与入口指针；已有缺失链接和本次引入的断链分别处理。

## 索引

更新受影响目录的 `index.md`，必要时更新祖先索引。列本层笔记和下一级目录，保留准确的人工分组和说明；规则条目说明何时读取。新增内容目录有索引，纯附件目录不强制。

索引不列自身，根索引不展开全库所有笔记。普通目录索引不带属性；根索引可声明实际采用的 OKF 版本。

## 日志

在 [根日志]({{log_path}}) 记录重要变化。按真实日期 `YYYY-MM-DD` 分组，新日期在前；一次任务合并一条，说明变化、原因并链接相关笔记。

新增、删除、移动、重要结论和规则／画像变更应记录；错字、排版及无变化的重复执行不单独记录。追加本次历史，保留已有条目。

## 画像与规则调整

用户要求更新，或出现可核实的稳定新信息时，主动检查 [USER.md]({{user_path}}) 与 [SOUL.md]({{soul_path}})，有依据时更新相应内容并记录变化。推测、冲突或短期情绪不直接写成稳定事实；角色和原则调整保持与用户明确要求一致。

字段含义、目录或维护方式变化时，同步受影响规则、索引、查询和入口链接。用户新增规则时更新规则索引，不把全部细则复制进 `AGENTS.md`。

## 完成检查

检查受影响文件的属性可解析、索引条目与实际内容一致、链接目标正确、应记录的变化已进入日志，原正文和功能仍可使用。涉及删除或覆盖先保留可恢复内容；含义不明的冲突只暂停相应操作。

用户手动改动在下一次处理相关范围或显式检查时补齐；规则本身不提供后台监听。

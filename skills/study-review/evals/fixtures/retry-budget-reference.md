# Retry budget 参考说明

在本材料中，**retry budget** 指一次上层操作允许消耗的额外重试次数。首次尝试不消耗预算；预算为 2 时，最多发生 1 次首次尝试和 2 次重试。

预算由整个上层操作共享，而不是每个嵌套调用各自重新获得。只有被分类为暂时性故障的失败可以消耗预算后重试；永久性故障应立即返回。等待退避不会恢复已经消耗的预算。

这个机制的目的，是在暂时性故障下提高成功机会，同时限制重试放大的负载与延迟。判断是否应该再次尝试时，需要同时检查故障类别、剩余预算和上层操作是否仍在截止时间内。

# 我对一次界面卡顿的分析

现象：用户点击“生成报告”后，页面大约 4 秒无法响应，但网络请求本身只用了 200 毫秒。

我的判断：这不是网络太慢，而是请求返回后的一段同步数据处理占住了主线程。计时日志显示，响应回来后立即执行了一个遍历 20 万条记录的循环；循环结束后，点击事件和重绘才继续发生。

我的处理：把计算拆成多个小批次，每批结束后用 `setTimeout(..., 0)` 让出执行权。修改后页面可以响应点击，但整项计算变成约 5 秒。我还不确定为什么总计算时间变长，也没有比较 Web Worker。
